import { REVIEWER_GROUP } from '../../constants/userContants';

describe('REVIEWER_GROUP constant', () => {
  test('should have the expected value', () => {
    expect(REVIEWER_GROUP).toBe('formsflow/formsflow-reviewer');
  });
});
